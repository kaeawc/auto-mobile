name: "Ensure iOS Simulator Runtime"
description: "Detect missing iOS simulator runtimes and install them if needed"

runs:
  using: "composite"
  steps:
    - name: "Ensure iOS Simulator runtime"
      shell: bash
      run: ./scripts/ios/ensure-simulator-runtime.sh
