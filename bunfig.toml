[test]
# Installs process-wide test defaults before any test file imports production
# singletons. This currently neutralizes telemetry writes so fire-and-forget
# recording cannot touch the real file-backed DB during tests (issue #3084).
preload = ["./test/setup/testPreload.ts"]
coverageReporter = ["text", "lcov"]
coverageDir = "./coverage"
